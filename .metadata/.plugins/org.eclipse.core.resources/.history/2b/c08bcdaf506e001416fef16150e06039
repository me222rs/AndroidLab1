package com.example.bmi;

import android.widget.EditText;

public class Calculate {
	public float convert(weight,length) {
		int length2 = length * length;
		int bmi = length2 / weight;
		return bmi;
	}
}
